# Non-sensitive runtime config for dokandar-recommendation -> ConfigMap `dokandar-recommendation-config` in namespace `backend`
APP_ENV=prod
TENANT=cloud
LOG_LEVEL=info
SERVICE_NAME=16-recommendation
