# dokandar-support → Secret `dokandar-support-secret` in namespace `support`
# Copy to dokandar-support.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/00-support/env/ (canonical source).
REDIS_URL=
MONGODB_URL=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
