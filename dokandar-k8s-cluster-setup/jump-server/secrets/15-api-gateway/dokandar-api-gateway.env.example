# dokandar-api-gateway → Secret `dokandar-api-gateway-secret` in namespace `gateway`
# Copy to dokandar-api-gateway.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/15-api-gateway/env/ (canonical source).
REDIS_URL=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
