# dokandar-frontend → Secret `dokandar-frontend-secret` in namespace `frontend`
# Copy to dokandar-frontend.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/ui-frontend/env/ (canonical source).
SESSION_COOKIE_SECRET=
