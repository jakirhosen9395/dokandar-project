# dokandar-notification → Secret `dokandar-notification-secret` in namespace `backend`
# Copy to dokandar-notification.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/14-notification/env/ (canonical source).
RABBITMQ_URL=
REDIS_URL=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
