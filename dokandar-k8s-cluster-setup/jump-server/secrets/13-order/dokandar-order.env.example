# dokandar-order → Secret `dokandar-order-secret` in namespace `backend`
# Copy to dokandar-order.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/13-order/env/ (canonical source).
DATABASE_URL=
MONGODB_URL=
SCYLLA_HOSTS=
TEMPORAL_ADDRESS=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
