# dokandar-shipping → Secret `dokandar-shipping-secret` in namespace `backend`
# Copy to dokandar-shipping.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/17-shipping/env/ (canonical source).
DATABASE_URL=
MONGODB_URL=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
