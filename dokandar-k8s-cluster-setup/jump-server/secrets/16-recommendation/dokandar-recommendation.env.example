# dokandar-recommendation → Secret `dokandar-recommendation-secret` in namespace `backend`
# Copy to dokandar-recommendation.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/16-recommendation/env/ (canonical source).
DATABASE_URL=
QDRANT_URL=
QDRANT_API_KEY=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
