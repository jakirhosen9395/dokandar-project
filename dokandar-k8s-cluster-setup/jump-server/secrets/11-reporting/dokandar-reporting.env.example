# dokandar-reporting → Secret `dokandar-reporting-secret` in namespace `backend`
# Copy to dokandar-reporting.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/11-reporting/env/ (canonical source).
CLICKHOUSE_URL=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
