# dokandar-risk-trust → Secret `dokandar-risk-trust-secret` in namespace `backend`
# Copy to dokandar-risk-trust.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/18-risk-trust/env/ (canonical source).
NEO4J_URI=
NEO4J_PASSWORD=
CLICKHOUSE_URL=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
