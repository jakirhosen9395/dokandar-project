# dokandar-auth → Secret `dokandar-auth-secret` in namespace `backend`
# Copy to dokandar-auth.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/01-auth/env/ (canonical source).
DATABASE_URL=
REDIS_URL=
KAFKA_BOOTSTRAP_SERVERS=
RABBITMQ_URL=
JWT_PRIVATE_KEY_B64=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
