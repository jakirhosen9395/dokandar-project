# dokandar-catalog → Secret `dokandar-catalog-secret` in namespace `backend`
# Copy to dokandar-catalog.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/04-catalog/env/ (canonical source).
DATABASE_URL=
REDIS_URL=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
