# dokandar-seller → Secret `dokandar-seller-secret` in namespace `backend`
# Copy to dokandar-seller.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/03-seller/env/ (canonical source).
DATABASE_URL=
REDIS_URL=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
