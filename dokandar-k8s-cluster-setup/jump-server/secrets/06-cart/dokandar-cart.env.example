# dokandar-cart → Secret `dokandar-cart-secret` in namespace `backend`
# Copy to dokandar-cart.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/06-cart/env/ (canonical source).
MONGODB_URL=
REDIS_URL=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
