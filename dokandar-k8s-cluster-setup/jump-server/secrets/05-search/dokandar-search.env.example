# dokandar-search → Secret `dokandar-search-secret` in namespace `backend`
# Copy to dokandar-search.env, fill the values (source: all_components_creds.txt /
# the service's env/init-env.sh output), then run ../apply-secrets.sh.
# TODO: reconcile this key list with backend/05-search/env/ (canonical source).
ELASTICSEARCH_URL=
MONGODB_URL=
JWT_PUBLIC_KEY_B64=
INTERNAL_SERVICE_TOKEN=
