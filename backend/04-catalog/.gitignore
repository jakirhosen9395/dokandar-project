# build
target/
*.class

# rendered env + real creds — NEVER commit (only the .example files are tracked)
env/.env.*
!env/.env.*.example
env/components-creds.txt
env/env.txt

# data snapshots (generated by collect.sh)
data/local/result.json
data/cloud/result.json

# smoke test output + local creds
smoke_test/out/
smoke_test/.env

# misc
.idea/
.vscode/
*.log
