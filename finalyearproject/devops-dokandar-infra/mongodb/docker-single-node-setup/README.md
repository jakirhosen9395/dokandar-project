# MongoDB — docker single node variant

```bash
bash setup_env.sh && bash setup.sh up    # credentials + PUBLIC url printed
bash setup.sh down|purge|status|logs
cd .. && bash test.sh
```

Public reach: `<server-ip>:27017` (SG tcp/27017 open, auth required).
