# RustFS — docker single node variant

```bash
bash setup_env.sh && bash setup.sh up   # prints access/secret keys + console URL
cd .. && bash test.sh
```
Public reach: console http://<server-ip>:9001/rustfs/console/index.html · S3 http://<server-ip>:9000.
