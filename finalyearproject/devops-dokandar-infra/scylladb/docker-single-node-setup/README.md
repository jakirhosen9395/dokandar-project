# ScyllaDB — docker single node variant

```bash
bash setup_env.sh && bash setup.sh up   # ~1 min to healthy (scylla bootstraps slowly)
cd .. && bash test.sh
```
Public reach: CQL <server-ip>:9042 (no auth — learning utility).
