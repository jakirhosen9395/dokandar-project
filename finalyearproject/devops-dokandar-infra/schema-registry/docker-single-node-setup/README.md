# Apicurio — docker single node variant (in-memory)

```bash
bash setup_env.sh && bash setup.sh up
cd .. && bash test.sh
```
Public reach: UI http://<server-ip>:8081/ui/ · API /apis/registry/v2 (no auth, in-mem).
