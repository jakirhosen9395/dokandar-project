{
  "name": "offline-sync-gateway",
  "version": "0.1.0",
  "description": "DOKANDAR edge — offline store-and-forward: replays a device's queued operations in order, idempotently (R8 offline-first)",
  "type": "module",
  "scripts": { "build": "tsc -p tsconfig.json", "test": "npm run build && node --test \"dist/test/**/*.test.js\"" },
  "devDependencies": { "@types/node": "^22.10.0", "typescript": "^5.7.0" }
}
