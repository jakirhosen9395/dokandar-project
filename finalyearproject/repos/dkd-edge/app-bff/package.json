{
  "name": "app-bff",
  "version": "0.1.0",
  "description": "DOKANDAR edge #34 — consumer aggregate views over the fleet's REST (stateless)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/test/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
