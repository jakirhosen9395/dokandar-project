apiVersion: v1
kind: Service
metadata: { name: logistics-svc, namespace: dkd-logistics }
spec:
  selector: { app: logistics-svc }
  ports: [{ name: http, port: 8080, targetPort: http }, { name: metrics, port: 9090, targetPort: metrics }]
