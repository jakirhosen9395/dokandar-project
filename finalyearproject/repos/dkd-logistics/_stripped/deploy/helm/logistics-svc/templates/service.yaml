apiVersion: v1
kind: Service
metadata:
  name: {{ include "logistics-svc.fullname" . }}
  labels: { app: logistics-svc }
spec:
  selector: { app: logistics-svc }
  ports:
    - { name: http, port: {{ .Values.service.port }}, targetPort: http }
    - { name: metrics, port: {{ .Values.service.metricsPort }}, targetPort: metrics }
