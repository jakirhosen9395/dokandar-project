apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "identity-svc.fullname" . }}-config
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
  DKD_SERVICE_NAME: identity-svc
  DKD_CONTEXT: identity
  DKD_HTTP_PORT: "8080"
