apiVersion: apps/v1
kind: Deployment
metadata: { name: identity-svc, namespace: dkd-identity, labels: { app: identity-svc } }
spec:
  replicas: 2
  selector: { matchLabels: { app: identity-svc } }
  template:
    metadata: { labels: { app: identity-svc }, annotations: { prometheus.io/scrape: "true", prometheus.io/port: "9090" } }
    spec:
      containers:
        - name: identity-svc
          image: registry.gitlab.com/final-year-project3354127/identity-svc:0.1.0
          ports: [{ name: http, containerPort: 8080 }, { name: metrics, containerPort: 9090 }]
          livenessProbe:  { httpGet: { path: /live,  port: http } }
          readinessProbe: { httpGet: { path: /ready, port: http } }
