apiVersion: v1
kind: Service
metadata: { name: identity-svc, namespace: dkd-identity }
spec:
  selector: { app: identity-svc }
  ports: [{ name: http, port: 8080, targetPort: http }, { name: metrics, port: 9090, targetPort: metrics }]
