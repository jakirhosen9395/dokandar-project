# Makes `ansible-playbook setup.yml` (run from this dir) auto-load the
# Terraform-generated inventory — no `-i inventory.ini` needed. The jump server
# is the only host; connect the VPN first.
[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False

[ssh_connection]
pipelining = True
